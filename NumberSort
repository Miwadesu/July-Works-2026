#include <stdio.h>
int main() {
int temp, num[5] = {67, 69, 11, 9, 18};
printf("\nBefore:\n%d\t%d\t%d\t%d\t%d", num[0], num[1], num[2], num[3], num[4]), num[5];
for (int pass = 0; pass < 4; pass++){
    for(int i = 0; i < 4; i++){
        if(num[i] > num[i+1]){
        temp =  num[i];
        num[i] = num[i+1];
        num[i+1] = temp;
        }
    }
        printf("\nAfter:\n%d\t%d\t%d\t%d\t%d", num[0], num[1], num[2], num[3], num[4]);
}
    return 0;
}
